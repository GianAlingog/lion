[package]
name = "arena"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
bot = { path = "../bot" }
engine = { path = "../engine" }

[lints]
workspace = true
