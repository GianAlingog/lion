[package]
name = "viewer"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
arena = { path = "../arena" }
bot = { path = "../bot" }
engine = { path = "../engine" }
ratatui = "0.30.2"

[lints]
workspace = true
